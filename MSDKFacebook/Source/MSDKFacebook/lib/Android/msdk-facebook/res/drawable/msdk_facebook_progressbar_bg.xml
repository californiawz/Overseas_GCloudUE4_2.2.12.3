<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--  设置背景色  -->
    <item android:id="@android:id/background"
        android:drawable="@color/msdk_facebook_progressbar_bg">
    </item>

    <!--  设置进度条颜色  -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:endColor="@color/msdk_facebook_progressbar_gradient_start"
                    android:startColor="@color/msdk_facebook_progressbar_gradient_end" />
            </shape>
        </clip>
    </item>
</layer-list>